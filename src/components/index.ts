// Barrel file — re-exporta componentes para imports limpos
// Uso: import { Button, AppShell } from '@/components';

export { default as Button } from './ui/Button.svelte';
export { default as AppShell } from './layout/AppShell.svelte';
